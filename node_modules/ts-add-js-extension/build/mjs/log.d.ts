export default class Log {
    private readonly numberOfFiles;
    private completedFiles;
    static readonly fromNumberOfFiles: (numberOfFiles: number) => Log;
    private constructor();
    private readonly redify;
    private readonly cyanify;
    private readonly boldify;
    readonly increment: (props: Readonly<{
        file: string;
        repeat: number;
        succeed: boolean;
    }>) => void;
    readonly end: (props: Readonly<{
        errors: ReadonlyArray<Readonly<{
            file: string;
            error: NodeJS.ErrnoException;
        }>>;
    }>) => void;
}
